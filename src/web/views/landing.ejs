<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: #000000;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at center, rgba(255, 140, 0, 0.1) 0%, transparent 70%);
            animation: pulse 10s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.1); opacity: 0.3; }
        }

        .container {
            background: linear-gradient(135deg, rgba(20, 20, 20, 0.95) 0%, rgba(10, 10, 10, 0.95) 100%);
            border: 1px solid rgba(255, 140, 0, 0.3);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5), 0 0 40px rgba(255, 140, 0, 0.1);
            padding: 40px;
            max-width: 450px;
            width: 100%;
            text-align: center;
            animation: slideUp 0.5s ease-out;
            position: relative;
            backdrop-filter: blur(10px);
        }
        
        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .logo {
            font-size: 60px;
            margin-bottom: 20px;
            filter: drop-shadow(0 0 20px rgba(255, 140, 0, 0.5));
        }

        .brand {
            font-size: 14px;
            color: #FF8C00;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 10px;
            font-weight: 600;
        }
        
        h1 {
            color: #FF8C00;
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 0 0 20px rgba(255, 140, 0, 0.5);
        }

        .subtitle {
            color: #ffffff;
            font-size: 16px;
            margin-bottom: 30px;
            line-height: 1.5;
            opacity: 0.9;
        }
        
        .buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .btn {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 18px 30px;
            border-radius: 12px;
            text-decoration: none;
            font-size: 16px;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
            position: relative;
            overflow: hidden;
        }
        
        .btn:before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.2);
            transition: left 0.5s ease;
        }
        
        .btn:hover:before {
            left: 100%;
        }
        
        .btn-whatsapp {
            background: linear-gradient(135deg, #25D366 0%, #128C7E 100%);
            color: white;
        }
        
        .btn-whatsapp:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(37, 211, 102, 0.3);
        }
        
        .btn-telegram {
            background: linear-gradient(135deg, #2AABEE 0%, #229ED9 100%);
            color: white;
        }
        
        .btn-telegram:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(42, 171, 238, 0.3);
        }
        
        .btn-icon {
            margin-right: 10px;
            font-size: 20px;
        }
        
        .features {
            display: flex;
            justify-content: space-around;
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 140, 0, 0.2);
        }

        .feature {
            text-align: center;
        }

        .feature-icon {
            font-size: 30px;
            margin-bottom: 8px;
            filter: drop-shadow(0 0 10px rgba(255, 140, 0, 0.3));
        }

        .feature-text {
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
        }

        .partner-info {
            margin-top: 20px;
            padding: 15px;
            background: rgba(255, 140, 0, 0.05);
            border: 1px solid rgba(255, 140, 0, 0.2);
            border-radius: 10px;
            font-size: 14px;
            color: rgba(255, 255, 255, 0.8);
        }
        
        @media (max-width: 480px) {
            .container {
                padding: 30px 20px;
            }
            
            h1 {
                font-size: 24px;
            }
            
            .subtitle {
                font-size: 14px;
            }
            
            .btn {
                padding: 16px 24px;
                font-size: 15px;
            }
        }
        
        .loading {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 9999;
            align-items: center;
            justify-content: center;
        }
        
        .loading.active {
            display: flex;
        }
        
        .spinner {
            width: 50px;
            height: 50px;
            border: 5px solid #f3f3f3;
            border-top: 5px solid #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="loading" id="loading">
        <div class="spinner"></div>
    </div>
    
    <div class="container">
        <div class="logo">🏎️</div>
        <div class="brand">SHIBA CARS PHUKET</div>
        <h1>Премиум Аренда Авто</h1>
        <p class="subtitle">Выберите удобный способ связи с нами</p>

        <div class="buttons">
            <a href="#" class="btn btn-whatsapp" onclick="trackAndRedirect('whatsapp'); return false;">
                <span class="btn-icon">📱</span>
                WhatsApp
            </a>

            <a href="#" class="btn btn-telegram" onclick="trackAndRedirect('telegram'); return false;">
                <span class="btn-icon">✈️</span>
                Telegram
            </a>
        </div>

        <div class="features">
            <div class="feature">
                <div class="feature-icon">🏆</div>
                <div class="feature-text">Премиум</div>
            </div>
            <div class="feature">
                <div class="feature-icon">⚡</div>
                <div class="feature-text">Надежно</div>
            </div>
            <div class="feature">
                <div class="feature-icon">💰</div>
                <div class="feature-text">Выгодно</div>
            </div>
        </div>
        
        <% if (partnerCode) { %>
        <div class="partner-info">
            🤝 Вы пришли от нашего партнера
        </div>
        <% } %>
    </div>
    
    <script>
        const clickId = '<%= clickId %>';
        const whatsappNumber = '<%= whatsappNumber %>';
        const telegramBot = '<%= telegramBot %>';
        const whatsappMessage = '<%= whatsappMessage %>';
        const telegramMessage = '<%= telegramMessage %>';
        
        function trackAndRedirect(type) {
            const loading = document.getElementById('loading');
            loading.classList.add('active');
            
            fetch('/api/redirect', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    clickId: clickId,
                    type: type
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    let url;
                    if (type === 'whatsapp') {
                        const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
                        const waUrl = isMobile ? 'whatsapp://send' : 'https://wa.me';
                        url = `${waUrl}/${whatsappNumber}?text=${encodeURIComponent(whatsappMessage)}`;
                    } else {
                        url = `https://t.me/${telegramBot}?start=from_partner`;
                    }
                    window.location.href = url;
                }
            })
            .catch(error => {
                console.error('Error:', error);
                loading.classList.remove('active');
                alert('Произошла ошибка. Пожалуйста, попробуйте позже.');
            });
        }
        
        setTimeout(() => {
            fetch('/api/track-view', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    clickId: clickId
                })
            });
        }, 1000);
    </script>
</body>
</html>