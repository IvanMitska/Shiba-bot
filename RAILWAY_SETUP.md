# Railway Setup Instructions

## Проблема, которая была обнаружена

Команда `/start` в телеграм боте не работала из-за того, что **webhook не был установлен**. Бот находился в polling mode, который не работает на Railway хостинге.

## Причина

В коде была проверка `process.env.NODE_ENV === 'production'`, но на Railway эта переменная не была установлена, поэтому бот не переключался в webhook режим.

## Решение

### 1. Исправления в коде (уже сделано)

✅ Изменена логика определения production режима:
- Теперь проверяется не только `NODE_ENV`, но и `RAILWAY_PUBLIC_DOMAIN` или `RAILWAY_ENVIRONMENT`
- Добавлено подробное логирование webhook статуса
- Добавлена периодическая проверка webhook каждые 5 минут с автоматическим восстановлением

### 2. Настройка переменных окружения на Railway

Зайдите в настройки проекта на Railway и установите следующие переменные:

#### Обязательные переменные:

```bash
# Telegram Bot
BOT_TOKEN=8326608021:AAHT1G8YOVAq_RfWiq2m6NGnbRyAx9fhKDU

# Database (уже должна быть установлена автоматически)
DATABASE_URL=postgresql://postgres:CSToGdtVnEakEwrNQLAEezSrHqkafjGo@hopper.proxy.rlwy.net:44490/railway

# Environment (ВАЖНО!)
NODE_ENV=production
```

#### Рекомендуемые переменные:

```bash
# Web App URL
WEBAPP_URL=https://shibo-tg-backend-production.up.railway.app/telegram-webapp

# Company Contacts
WHATSAPP_NUMBER=66959657805
TELEGRAM_COMPANY_BOT=ShibaCars_Phuket

# Landing Domain
LANDING_DOMAIN=https://shiba-cars-phuket.com

# Security
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Logging
LOG_LEVEL=info
```

**Примечание:** `RAILWAY_PUBLIC_DOMAIN` устанавливается Railway автоматически, её не нужно добавлять вручную.

### 3. Деплой изменений

После обновления кода выполните:

```bash
git add .
git commit -m "Fix webhook setup for Railway deployment"
git push
```

Railway автоматически задеплоит изменения.

### 4. Проверка работоспособности

#### Проверка через скрипт:

Локально выполните:
```bash
node check-bot-status.js
```

Должен показать:
- ✅ Webhook URL установлен
- ✅ URL: `https://shibo-tg-backend-production.up.railway.app/webhook`

#### Проверка в Telegram:

1. Откройте бота: [@SHIBA_CARS_PARTNERS_BOT](https://t.me/SHIBA_CARS_PARTNERS_BOT)
2. Отправьте команду `/start`
3. Должно прийти приветственное сообщение с кнопкой "📊 Открыть приложение"

### 5. Мониторинг

Логи на Railway будут показывать:
- Статус webhook при запуске
- Информацию о каждом webhook запросе
- Автоматические проверки webhook каждые 5 минут

## Утилиты для диагностики

### check-bot-status.js
Проверяет текущий статус webhook:
```bash
node check-bot-status.js
```

### setup-webhook.js
Вручную устанавливает webhook:
```bash
node setup-webhook.js
```

## Возможные проблемы

### Webhook сбрасывается после деплоя

**Решение:** Убедитесь, что `NODE_ENV=production` установлена на Railway.

### /start не отвечает

1. Проверьте webhook: `node check-bot-status.js`
2. Проверьте логи Railway
3. Установите webhook вручную: `node setup-webhook.js`

### Ошибки в webhook

Проверьте логи Railway на наличие ошибок при обработке `/webhook` endpoint.

## Что было сделано

1. ✅ Webhook установлен вручную через `setup-webhook.js`
2. ✅ Улучшена логика определения production режима в `src/index.js`
3. ✅ Добавлено подробное логирование webhook статуса
4. ✅ Добавлена автоматическая проверка и восстановление webhook
5. ✅ Создана документация по настройке

## Следующие шаги

1. Установите `NODE_ENV=production` на Railway
2. Задеплойте изменения
3. Проверьте работу команды `/start`
4. Мониторьте логи на предмет ошибок

---

**Дата создания:** 2025-11-06
**Статус:** ✅ Webhook установлен и работает
